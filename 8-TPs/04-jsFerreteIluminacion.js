/*4.	Para el departamento de iluminación:
Tomando en cuenta que todas las lámparas están en oferta al mismo precio de $35 pesos final.
 
B.	Si compra 5  lamparitas bajo consumo marca "ArgentinaLuz" se hace un descuento del 40 % y si es de otra marca el descuento es del 30%.
C.	Si compra 4  lamparitas bajo consumo marca "ArgentinaLuz" o “FelipeLamparas” se hace un descuento del 25 % y si es de otra marca el descuento es del 20%.
D.	Si compra 3  lamparitas bajo consumo marca "ArgentinaLuz"  el descuento es del 15%, si es  “FelipeLamparas” se hace un descuento del 10 % y si es de otra marca un 5%.
E.	Si el importe final con descuento suma más de $120  se debe sumar un 10% de ingresos brutos en informar del impuesto con el siguiente mensaje:
 ”Usted pago X de IIBB.”, siendo X el impuesto que se pagó. 
 */
function CalcularPrecio () 
{
 	






































    /*var Preciolamparas = 35;
    var marcas;
    var cantLamp;
    var importeSinDesc;
    var importeConDesc = 0;
    var precioFinal;
    var importeFinConImpu = 0;

    importeSinDesc = cantLamp * Preciolamparas
    //importeConDesc = (cantLamp * Preciolamparas) * 0.50;

    cantLamp = parseInt(document.getElementById("txtIdCantidad").value);

    if(marcas == "ArgentinaLuz" && cantLamp == 5)
    {
        importeConDesc = (cantLamp * Preciolamparas) * 0.40;
    }
    else if((marcas == "FelipeLamparas" || marcas == "ArgentinaLuz") && cantLamp == 4)
    {
        importeConDesc = (cantLamp * Preciolamparas) * 0.25;
    }
    if(cantLamp == 3) 
    {
        importeConDesc = (cantLamp * Preciolamparas) * 0.05;
        if(marcas == "ArgentinaLuz")
        {
            importeConDesc = (cantLamp * Preciolamparas) * 0.15;
        }
        if(marcas == "FelipeLamparas") 
        {
            importeConDesc = (cantLamp * Preciolamparas) * 0.10;
        }
        
    }
    else if(marcas == "")
    {
        

    }
    if(importeConDesc > 120)
    {
        importeFinConImpu = importeConDesc + importeConDesc * 0.10;
         document.getElementById("txtIdprecioDescuento").value = importeFinConImpu;
         alert("IIBB Usted pago $ " + importeConDesc + " siendo $ " + importeFinConImpu + " el impuesto que se pagó.");
    }*/      
    
}